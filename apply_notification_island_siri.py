#!/usr/bin/env python3
from pathlib import Path
import sys

SOURCE = '//\n//  notificationisland.m\n//  Cyanide\n//\n//  Siri-style Notification Island presentation:\n//  - Reads the currently presented SpringBoard notification request.\n//  - Portrait: replaces the banner visually with a SpringBoard-owned overlay.\n//  - Landscape: leaves the native banner completely untouched.\n//  - The top capsule is black; notification content below it is transparent.\n//  - A high-level, small UIWindow visually covers any existing Dynamic Island\n//    content without stopping or mutating the underlying Live Activity.\n//  - Swipe up dismisses only this presentation layer.\n//\n\n#import "notificationisland.h"\n#import "../remote_objc.h"\n#import "../../TaskRop/RemoteCall.h"\n#import "../../LogTextView.h"\n\n#import <Foundation/Foundation.h>\n#import <objc/message.h>\n#import <math.h>\n#import <stdio.h>\n#import <string.h>\n#import <sys/time.h>\n\n#pragma mark - Geometry / constants\n\ntypedef struct { double x, y; } NIPoint64;\ntypedef struct { double width, height; } NISize64;\ntypedef struct { NIPoint64 origin; NISize64 size; } NIRect64;\ntypedef struct { double top, left, bottom, right; } NIEdgeInsets64;\n\ntypedef struct {\n    const void *data;\n    size_t size;\n} NIInvocationArg;\n\nstatic const uint64_t kNIVisibleUS = 3600000ULL;\nstatic const uint64_t kNIRepeatGuardUS = 1200000ULL;\nstatic const double kNIWindowLevel = 999998.0;\nstatic const double kNIShowFadeSeconds = 0.18;\nstatic const double kNIHideFadeSeconds = 0.16;\n\nstatic const uint64_t kNITagIsland = 41001;\nstatic const uint64_t kNITagIcon   = 41002;\nstatic const uint64_t kNITagSource = 41003;\nstatic const uint64_t kNITagTitle  = 41004;\nstatic const uint64_t kNITagBody   = 41005;\n\nstatic const char *kNIWindowAssocKey = "cyanideNotificationIslandWindow";\nstatic const char *kNIHideInvocationAssocKey = "cyanideNotificationIslandHideInvocation";\n\nstatic bool gNIOverlayPrepared = false;\nstatic uint64_t gNILastRequest = 0;\nstatic uint64_t gNIVisibleUntilUS = 0;\nstatic uint64_t gNILastShowUS = 0;\nstatic uint64_t gNILastContentHash = 0;\nstatic char gNILastIdentifier[192] = {0};\nstatic int gNILastFrontOrientation = 1;\nstatic bool gNIMissingBannerLogged = false;\nstatic bool gNIMissingDispatcherLogged = false;\n\nstatic uint64_t ni_now_us(void)\n{\n    struct timeval tv;\n    gettimeofday(&tv, NULL);\n    return ((uint64_t)tv.tv_sec * 1000000ULL) + (uint64_t)tv.tv_usec;\n}\n\nstatic double ni_clamp(double value, double lo, double hi)\n{\n    if (value < lo) return lo;\n    if (value > hi) return hi;\n    return value;\n}\n\nstatic uint64_t ni_hash_bytes(uint64_t hash, const char *s)\n{\n    if (!hash) hash = 1469598103934665603ULL;\n    if (!s) return hash;\n    for (const unsigned char *p = (const unsigned char *)s; *p; ++p) {\n        hash ^= (uint64_t)*p;\n        hash *= 1099511628211ULL;\n    }\n    return hash;\n}\n\nstatic uint64_t ni_content_hash(const char *source, const char *title, const char *body)\n{\n    uint64_t h = 1469598103934665603ULL;\n    h = ni_hash_bytes(h, source);\n    h ^= 0xff; h *= 1099511628211ULL;\n    h = ni_hash_bytes(h, title);\n    h ^= 0xfe; h *= 1099511628211ULL;\n    h = ni_hash_bytes(h, body);\n    return h;\n}\n\n#pragma mark - Small RemoteCall helpers\n\nstatic uint64_t ni_try_msg0_main(uint64_t obj, const char *selName)\n{\n    if (!r_is_objc_ptr(obj) || !r_responds_main(obj, selName)) return 0;\n    return r_msg2_main(obj, selName, 0, 0, 0, 0);\n}\n\nstatic void ni_send_double_main(uint64_t obj, const char *selName, double value)\n{\n    if (!r_is_objc_ptr(obj) || !selName) return;\n    r_msg2_main_raw(obj, selName,\n                    &value, sizeof(value),\n                    NULL, 0, NULL, 0, NULL, 0);\n}\n\nstatic void ni_send_float_main(uint64_t obj, const char *selName, float value)\n{\n    if (!r_is_objc_ptr(obj) || !selName) return;\n    r_msg2_main_raw(obj, selName,\n                    &value, sizeof(value),\n                    NULL, 0, NULL, 0, NULL, 0);\n}\n\nstatic void ni_send_rect_main(uint64_t obj, const char *selName, NIRect64 rect)\n{\n    if (!r_is_objc_ptr(obj) || !selName) return;\n    r_msg2_main_raw(obj, selName,\n                    &rect, sizeof(rect),\n                    NULL, 0, NULL, 0, NULL, 0);\n}\n\nstatic void ni_send_size_main(uint64_t obj, const char *selName, NISize64 size)\n{\n    if (!r_is_objc_ptr(obj) || !selName) return;\n    r_msg2_main_raw(obj, selName,\n                    &size, sizeof(size),\n                    NULL, 0, NULL, 0, NULL, 0);\n}\n\nstatic uint64_t ni_class_msg_double(uint64_t cls, const char *selName, double value)\n{\n    if (!r_is_objc_ptr(cls) || !selName) return 0;\n    return r_msg2_main_raw(cls, selName,\n                           &value, sizeof(value),\n                           NULL, 0, NULL, 0, NULL, 0);\n}\n\nstatic uint64_t ni_color_white_alpha(double white, double alpha)\n{\n    uint64_t UIColor = r_class("UIColor");\n    if (!r_is_objc_ptr(UIColor)) return 0;\n    return r_msg2_main_raw(UIColor, "colorWithWhite:alpha:",\n                           &white, sizeof(white),\n                           &alpha, sizeof(alpha),\n                           NULL, 0, NULL, 0);\n}\n\nstatic uint64_t ni_font(double size, bool bold)\n{\n    uint64_t UIFont = r_class("UIFont");\n    if (!r_is_objc_ptr(UIFont)) return 0;\n    return ni_class_msg_double(UIFont,\n                               bold ? "boldSystemFontOfSize:" : "systemFontOfSize:",\n                               size);\n}\n\nstatic void ni_release_remote(uint64_t obj)\n{\n    if (r_is_objc_ptr(obj) && r_responds_main(obj, "release")) {\n        r_msg2_main(obj, "release", 0, 0, 0, 0);\n    }\n}\n\nstatic uint64_t ni_assoc_get(uint64_t owner, const char *keyName)\n{\n    if (!r_is_objc_ptr(owner) || !keyName) return 0;\n    uint64_t key = r_sel(keyName);\n    if (!key) return 0;\n    return r_dlsym_call(R_TIMEOUT, "objc_getAssociatedObject",\n                        owner, key, 0, 0, 0, 0, 0, 0);\n}\n\nstatic bool ni_assoc_set(uint64_t owner, const char *keyName, uint64_t obj)\n{\n    if (!r_is_objc_ptr(owner) || !keyName) return false;\n    uint64_t key = r_sel(keyName);\n    if (!key) return false;\n    r_dlsym_call(R_TIMEOUT, "objc_setAssociatedObject",\n                 owner, key, obj, 1 /* OBJC_ASSOCIATION_RETAIN_NONATOMIC */,\n                 0, 0, 0, 0);\n    return true;\n}\n\n#pragma mark - NSInvocation target for swipe-to-dismiss\n\nstatic bool ni_set_invocation_arg(uint64_t invocation,\n                                  uint64_t index,\n                                  const void *arg,\n                                  size_t argSize)\n{\n    if (!r_is_objc_ptr(invocation)) return false;\n    size_t len = argSize > 8 ? argSize : 8;\n    uint64_t buf = r_dlsym_call(R_TIMEOUT, "malloc", len, 0, 0, 0, 0, 0, 0, 0);\n    if (!buf) return false;\n\n    bool ok = false;\n    if (argSize <= 8) {\n        uint64_t value = 0;\n        if (arg && argSize) memcpy(&value, arg, argSize);\n        ok = remote_write64(buf, value);\n    } else {\n        ok = arg && remote_write(buf, arg, argSize);\n    }\n    if (ok) {\n        r_msg2_main(invocation, "setArgument:atIndex:", buf, index, 0, 0);\n    }\n    r_free(buf);\n    return ok;\n}\n\nstatic uint64_t ni_invocation(uint64_t target,\n                              const char *selectorName,\n                              const NIInvocationArg *args,\n                              size_t argCount)\n{\n    if (!r_is_objc_ptr(target) || !selectorName) return 0;\n    uint64_t selector = r_sel(selectorName);\n    if (!selector || !r_responds_main(target, selectorName)) return 0;\n\n    uint64_t signature = r_msg2_main(target, "methodSignatureForSelector:",\n                                     selector, 0, 0, 0);\n    if (!r_is_objc_ptr(signature)) return 0;\n    uint64_t reported = r_msg2_main(signature, "numberOfArguments", 0, 0, 0, 0);\n    if (reported < argCount + 2) return 0;\n\n    uint64_t NSInvocation = r_class("NSInvocation");\n    uint64_t invocation = r_is_objc_ptr(NSInvocation)\n        ? r_msg2_main(NSInvocation, "invocationWithMethodSignature:", signature, 0, 0, 0)\n        : 0;\n    if (!r_is_objc_ptr(invocation)) return 0;\n\n    uint64_t retained = r_msg2_main(invocation, "retain", 0, 0, 0, 0);\n    if (r_is_objc_ptr(retained)) invocation = retained;\n    r_msg2_main(invocation, "setTarget:", target, 0, 0, 0);\n    r_msg2_main(invocation, "setSelector:", selector, 0, 0, 0);\n\n    for (size_t i = 0; i < argCount; i++) {\n        if (!ni_set_invocation_arg(invocation, i + 2, args[i].data, args[i].size)) {\n            ni_release_remote(invocation);\n            return 0;\n        }\n    }\n    r_msg2_main(invocation, "retainArguments", 0, 0, 0, 0);\n    return invocation;\n}\n\n#pragma mark - SpringBoard / orientation\n\nstatic uint64_t ni_springboard_application(void)\n{\n    uint64_t UIApplication = r_class("UIApplication");\n    if (!r_is_objc_ptr(UIApplication)) return 0;\n    return r_msg2_main(UIApplication, "sharedApplication", 0, 0, 0, 0);\n}\n\nstatic uint64_t ni_key_window(uint64_t app)\n{\n    if (!r_is_objc_ptr(app)) return 0;\n    uint64_t win = r_msg2_main(app, "keyWindow", 0, 0, 0, 0);\n    if (r_is_objc_ptr(win)) return win;\n\n    uint64_t windows = r_msg2_main(app, "windows", 0, 0, 0, 0);\n    uint64_t count = r_is_objc_ptr(windows) ? r_msg2_main(windows, "count", 0, 0, 0, 0) : 0;\n    if (count > 0 && count < 64) {\n        win = r_msg2_main(windows, "objectAtIndex:", 0, 0, 0, 0);\n    }\n    return win;\n}\n\nstatic int ni_frontmost_orientation(void)\n{\n    uint64_t app = ni_springboard_application();\n    if (!r_is_objc_ptr(app)) return gNILastFrontOrientation;\n\n    const char *selectors[] = {\n        "_frontMostAppOrientation",\n        "activeInterfaceOrientation",\n        "statusBarOrientation",\n    };\n    for (size_t i = 0; i < sizeof(selectors) / sizeof(selectors[0]); i++) {\n        if (!r_responds_main(app, selectors[i])) continue;\n        int orientation = (int)r_msg2_main(app, selectors[i], 0, 0, 0, 0);\n        if (orientation >= 1 && orientation <= 4) {\n            gNILastFrontOrientation = orientation;\n            return orientation;\n        }\n    }\n\n    uint64_t UIDevice = r_class("UIDevice");\n    uint64_t device = r_is_objc_ptr(UIDevice)\n        ? r_msg2_main(UIDevice, "currentDevice", 0, 0, 0, 0)\n        : 0;\n    if (r_is_objc_ptr(device) && r_responds_main(device, "orientation")) {\n        int orientation = (int)r_msg2_main(device, "orientation", 0, 0, 0, 0);\n        if (orientation >= 1 && orientation <= 4) {\n            gNILastFrontOrientation = orientation;\n        }\n    }\n    return gNILastFrontOrientation;\n}\n\nstatic bool ni_frontmost_is_landscape(void)\n{\n    uint32_t oldSettle = r_settle_us(2000);\n    int orientation = ni_frontmost_orientation();\n    r_settle_us(oldSettle);\n    return orientation == 3 || orientation == 4;\n}\n\n#pragma mark - Notification request extraction\n\nstatic uint64_t ni_springboard_dispatcher(uint64_t app)\n{\n    uint64_t dispatcher = ni_try_msg0_main(app, "notificationDispatcher");\n    if (!r_is_objc_ptr(dispatcher)) dispatcher = r_ivar_value(app, "_notificationDispatcher");\n    if (!r_is_objc_ptr(dispatcher) && !gNIMissingDispatcherLogged) {\n        printf("[NISLAND] SpringBoard notificationDispatcher unavailable\\n");\n        gNIMissingDispatcherLogged = true;\n    }\n    return dispatcher;\n}\n\nstatic uint64_t ni_banner_destination(uint64_t dispatcher)\n{\n    uint64_t dest = ni_try_msg0_main(dispatcher, "bannerDestination");\n    if (!r_is_objc_ptr(dest)) dest = r_ivar_value(dispatcher, "_bannerDestination");\n    if (!r_is_objc_ptr(dest)) dest = r_ivar_value(dispatcher, "_alertDestination");\n    if (!r_is_objc_ptr(dest) && !gNIMissingBannerLogged) {\n        printf("[NISLAND] bannerDestination unavailable dispatcher=0x%llx\\n", (unsigned long long)dispatcher);\n        gNIMissingBannerLogged = true;\n    }\n    return dest;\n}\n\nstatic uint64_t ni_active_request_from_presenter(uint64_t presenter)\n{\n    if (!r_is_objc_ptr(presenter)) return 0;\n    uint64_t req = ni_try_msg0_main(presenter, "notificationRequest");\n    if (!r_is_objc_ptr(req)) req = ni_try_msg0_main(presenter, "request");\n    if (!r_is_objc_ptr(req)) req = r_ivar_value(presenter, "_notificationRequest");\n    if (!r_is_objc_ptr(req)) req = r_ivar_value(presenter, "_request");\n    return req;\n}\n\nstatic uint64_t ni_active_notification_request(uint64_t banner)\n{\n    if (!r_is_objc_ptr(banner)) return 0;\n    uint64_t req = ni_try_msg0_main(banner, "presentedNotificationRequest");\n    if (!r_is_objc_ptr(req)) req = ni_try_msg0_main(banner, "_presentedNotificationRequest");\n    if (!r_is_objc_ptr(req)) req = r_ivar_value(banner, "_presentedNotificationRequest");\n    if (!r_is_objc_ptr(req)) req = r_ivar_value(banner, "_currentNotificationRequest");\n    if (!r_is_objc_ptr(req)) req = r_ivar_value(banner, "_notificationRequest");\n    if (r_is_objc_ptr(req)) return req;\n\n    uint64_t presenter = ni_try_msg0_main(banner, "presentedNotificationRequestPresenter");\n    if (!r_is_objc_ptr(presenter)) presenter = ni_try_msg0_main(banner, "_presentedNotificationRequestPresenter");\n    if (!r_is_objc_ptr(presenter)) presenter = r_ivar_value(banner, "_presentedNotificationRequestPresenter");\n    req = ni_active_request_from_presenter(presenter);\n    if (r_is_objc_ptr(req)) return req;\n\n    uint64_t queue = r_ivar_value(banner, "_alertQueue");\n    if (r_is_objc_ptr(queue)) {\n        req = r_ivar_value(queue, "_coalescingRequest");\n        if (!r_is_objc_ptr(req)) req = r_ivar_value(queue, "_activeRequest");\n    }\n    return req;\n}\n\nstatic bool ni_read_string_obj(uint64_t obj, char *out, size_t outLen)\n{\n    if (!out || outLen == 0) return false;\n    out[0] = \'\\0\';\n    if (!r_is_objc_ptr(obj)) return false;\n    if (r_read_nsstring(obj, out, outLen) && out[0]) return true;\n    uint64_t stringObj = ni_try_msg0_main(obj, "string");\n    return r_read_nsstring(stringObj, out, outLen) && out[0];\n}\n\nstatic bool ni_read_field(uint64_t obj, const char *ivarName,\n                          const char *selName, char *out, size_t outLen)\n{\n    if (!out || outLen == 0) return false;\n    out[0] = \'\\0\';\n    if (!r_is_objc_ptr(obj)) return false;\n\n    uint64_t value = 0;\n    if (ivarName) value = r_ivar_value(obj, ivarName);\n    if (!r_is_objc_ptr(value) && selName) value = ni_try_msg0_main(obj, selName);\n    return ni_read_string_obj(value, out, outLen);\n}\n\nstatic void ni_short_bundle_name(const char *bundle, char *out, size_t outLen)\n{\n    if (!out || outLen == 0) return;\n    out[0] = \'\\0\';\n    if (!bundle || !bundle[0]) {\n        snprintf(out, outLen, "%s", "Notification");\n        return;\n    }\n    const char *last = strrchr(bundle, \'.\');\n    snprintf(out, outLen, "%s", (last && last[1]) ? last + 1 : bundle);\n}\n\nstatic bool ni_request_identifier(uint64_t req, char *identifier, size_t identifierLen)\n{\n    if (!identifier || identifierLen == 0) return false;\n    identifier[0] = \'\\0\';\n    if (!r_is_objc_ptr(req)) return false;\n\n    if (ni_read_field(req, "_notificationIdentifier", "notificationIdentifier",\n                      identifier, identifierLen)) {\n        return true;\n    }\n    uint64_t uuid = r_ivar_value(req, "_uuid");\n    if (!r_is_objc_ptr(uuid)) uuid = ni_try_msg0_main(req, "uuid");\n    uint64_t uuidString = ni_try_msg0_main(uuid, "UUIDString");\n    if (ni_read_string_obj(uuidString, identifier, identifierLen)) return true;\n\n    snprintf(identifier, identifierLen, "ptr-%llx", (unsigned long long)req);\n    return true;\n}\n\nstatic uint64_t ni_request_content(uint64_t req)\n{\n    uint64_t content = r_ivar_value(req, "_content");\n    if (!r_is_objc_ptr(content)) content = ni_try_msg0_main(req, "content");\n    if (!r_is_objc_ptr(content)) {\n        uint64_t bulletin = r_ivar_value(req, "_bulletin");\n        if (!r_is_objc_ptr(bulletin)) bulletin = ni_try_msg0_main(req, "bulletin");\n        content = r_ivar_value(bulletin, "_content");\n    }\n    return content;\n}\n\nstatic bool ni_request_text(uint64_t req,\n                            char *identifier, size_t identifierLen,\n                            char *bundle, size_t bundleLen,\n                            char *source, size_t sourceLen,\n                            char *title, size_t titleLen,\n                            char *body, size_t bodyLen)\n{\n    if (!r_is_objc_ptr(req)) return false;\n    char localBundle[192] = {0};\n    char header[192] = {0};\n    char contentTitle[256] = {0};\n    char message[512] = {0};\n    char subtitle[256] = {0};\n\n    (void)ni_request_identifier(req, identifier, identifierLen);\n    (void)ni_read_field(req, "_sectionIdentifier", "sectionIdentifier",\n                        localBundle, sizeof(localBundle));\n\n    uint64_t content = ni_request_content(req);\n    (void)ni_read_field(content, "_header", "header", header, sizeof(header));\n    if (!header[0]) (void)ni_read_field(content, "_customHeader", "customHeader", header, sizeof(header));\n    if (!header[0]) (void)ni_read_field(content, "_defaultHeader", "defaultHeader", header, sizeof(header));\n    (void)ni_read_field(content, "_title", "title", contentTitle, sizeof(contentTitle));\n    (void)ni_read_field(content, "_subtitle", "subtitle", subtitle, sizeof(subtitle));\n    (void)ni_read_field(content, "_message", "message", message, sizeof(message));\n    if (!message[0]) (void)ni_read_field(content, "_attributedMessage", "attributedMessage", message, sizeof(message));\n\n    if (!header[0]) {\n        uint64_t bulletin = r_ivar_value(req, "_bulletin");\n        if (!r_is_objc_ptr(bulletin)) bulletin = ni_try_msg0_main(req, "bulletin");\n        (void)ni_read_field(bulletin, "_sectionDisplayName", "sectionDisplayName",\n                            header, sizeof(header));\n    }\n    if (!header[0]) ni_short_bundle_name(localBundle, header, sizeof(header));\n\n    if (bundle && bundleLen) snprintf(bundle, bundleLen, "%s", localBundle);\n    if (source && sourceLen) snprintf(source, sourceLen, "%s", header);\n    if (title && titleLen) {\n        snprintf(title, titleLen, "%s", contentTitle[0] ? contentTitle : header);\n    }\n    if (body && bodyLen) {\n        body[0] = \'\\0\';\n        if (message[0] && (!contentTitle[0] || strcmp(contentTitle, message) != 0)) {\n            snprintf(body, bodyLen, "%s", message);\n        } else if (subtitle[0]) {\n            snprintf(body, bodyLen, "%s", subtitle);\n        }\n    }\n    return title && title[0];\n}\n\n// Withdraw only from the banner destination. Deliberately avoid the global\n// Notification Center dispatcher so dismissing our presentation does not try to\n// delete the notification from Notification Center history.\nstatic bool ni_withdraw_banner_only(uint64_t banner, uint64_t req)\n{\n    if (!r_is_objc_ptr(banner) || !r_is_objc_ptr(req)) return false;\n    if (r_responds_main(banner, "withdrawNotificationRequest:")) {\n        r_msg2_main_async(banner, "withdrawNotificationRequest:", req, 0, 0, 0);\n        return true;\n    }\n    if (r_responds_main(banner, "withdrawNotificationRequest:completion:")) {\n        r_msg2_main_async(banner, "withdrawNotificationRequest:completion:", req, 0, 0, 0);\n        return true;\n    }\n    return false;\n}\n\n#pragma mark - Overlay construction\n\nstatic uint64_t ni_overlay_window(void)\n{\n    uint64_t app = ni_springboard_application();\n    return ni_assoc_get(app, kNIWindowAssocKey);\n}\n\nstatic uint64_t ni_overlay_root(uint64_t win)\n{\n    if (!r_is_objc_ptr(win)) return 0;\n    uint64_t vc = r_msg2_main(win, "rootViewController", 0, 0, 0, 0);\n    return r_is_objc_ptr(vc) ? r_msg2_main(vc, "view", 0, 0, 0, 0) : 0;\n}\n\nstatic uint64_t ni_overlay_tagged(uint64_t win, uint64_t tag)\n{\n    uint64_t root = ni_overlay_root(win);\n    if (!r_is_objc_ptr(root)) return 0;\n    return r_msg2_main(root, "viewWithTag:", tag, 0, 0, 0);\n}\n\nstatic void ni_label_shadow(uint64_t label)\n{\n    if (!r_is_objc_ptr(label)) return;\n    uint64_t UIColor = r_class("UIColor");\n    uint64_t black = r_is_objc_ptr(UIColor) ? r_msg2_main(UIColor, "blackColor", 0, 0, 0, 0) : 0;\n    if (r_is_objc_ptr(black)) r_msg2_main(label, "setShadowColor:", black, 0, 0, 0);\n    NISize64 offset = {0.0, 1.0};\n    ni_send_size_main(label, "setShadowOffset:", offset);\n}\n\nstatic uint64_t ni_make_label(NIRect64 frame, double size, bool bold, uint64_t color, uint64_t tag)\n{\n    uint64_t UILabel = r_class("UILabel");\n    uint64_t alloc = r_is_objc_ptr(UILabel) ? r_msg2_main(UILabel, "alloc", 0, 0, 0, 0) : 0;\n    uint64_t label = r_is_objc_ptr(alloc)\n        ? r_msg2_main_raw(alloc, "initWithFrame:", &frame, sizeof(frame), NULL, 0, NULL, 0, NULL, 0)\n        : 0;\n    if (!r_is_objc_ptr(label)) return 0;\n\n    uint64_t font = ni_font(size, bold);\n    if (r_is_objc_ptr(font)) r_msg2_main(label, "setFont:", font, 0, 0, 0);\n    if (r_is_objc_ptr(color)) r_msg2_main(label, "setTextColor:", color, 0, 0, 0);\n    r_msg2_main(label, "setBackgroundColor:", r_msg2_main(r_class("UIColor"), "clearColor", 0, 0, 0, 0), 0, 0, 0);\n    r_msg2_main(label, "setTag:", tag, 0, 0, 0);\n    r_msg2_main(label, "setAdjustsFontSizeToFitWidth:", 1, 0, 0, 0);\n    ni_label_shadow(label);\n    return label;\n}\n\nstatic bool ni_cancel_scheduled_hide(uint64_t win)\n{\n    uint64_t hideInvocation = ni_assoc_get(win, kNIHideInvocationAssocKey);\n    if (!r_is_objc_ptr(hideInvocation)) return false;\n    uint64_t NSObject = r_class("NSObject");\n    uint64_t invokeSel = r_sel("invoke");\n    if (!r_is_objc_ptr(NSObject) || !invokeSel) return false;\n    r_msg2_main(NSObject,\n                "cancelPreviousPerformRequestsWithTarget:selector:object:",\n                hideInvocation, invokeSel, 0, 0);\n    return true;\n}\n\nstatic bool ni_schedule_hide(uint64_t win, double delay)\n{\n    uint64_t hideInvocation = ni_assoc_get(win, kNIHideInvocationAssocKey);\n    if (!r_is_objc_ptr(hideInvocation)) return false;\n    uint64_t invokeSel = r_sel("invoke");\n    uint64_t nilObject = 0;\n    if (!invokeSel) return false;\n    r_msg2_main_raw(hideInvocation,\n                    "performSelector:withObject:afterDelay:",\n                    &invokeSel, sizeof(invokeSel),\n                    &nilObject, sizeof(nilObject),\n                    &delay, sizeof(delay),\n                    NULL, 0);\n    return true;\n}\n\nstatic void ni_layer_opacity(uint64_t view, float opacity)\n{\n    if (!r_is_objc_ptr(view)) return;\n    uint64_t layer = r_msg2_main(view, "layer", 0, 0, 0, 0);\n    if (r_is_objc_ptr(layer)) ni_send_float_main(layer, "setOpacity:", opacity);\n}\n\nstatic void ni_layer_fade(uint64_t view, double from, double to, double duration)\n{\n    if (!r_is_objc_ptr(view)) return;\n    uint64_t layer = r_msg2_main(view, "layer", 0, 0, 0, 0);\n    uint64_t CABasicAnimation = r_class("CABasicAnimation");\n    uint64_t NSNumber = r_class("NSNumber");\n    if (!r_is_objc_ptr(layer) || !r_is_objc_ptr(CABasicAnimation) || !r_is_objc_ptr(NSNumber)) return;\n\n    uint64_t keyPath = r_nsstr_retained("opacity");\n    uint64_t anim = r_is_objc_ptr(keyPath)\n        ? r_msg2_main(CABasicAnimation, "animationWithKeyPath:", keyPath, 0, 0, 0)\n        : 0;\n    if (r_is_objc_ptr(keyPath)) ni_release_remote(keyPath);\n    if (!r_is_objc_ptr(anim)) return;\n\n    uint64_t fromNumber = ni_class_msg_double(NSNumber, "numberWithDouble:", from);\n    uint64_t toNumber = ni_class_msg_double(NSNumber, "numberWithDouble:", to);\n    if (r_is_objc_ptr(fromNumber)) r_msg2_main(anim, "setFromValue:", fromNumber, 0, 0, 0);\n    if (r_is_objc_ptr(toNumber)) r_msg2_main(anim, "setToValue:", toNumber, 0, 0, 0);\n    ni_send_double_main(anim, "setDuration:", duration);\n    r_msg2_main(anim, "setRemovedOnCompletion:", 1, 0, 0, 0);\n    uint64_t key = r_nsstr_retained("cyanide.notificationisland.opacity");\n    r_msg2_main(layer, "addAnimation:forKey:", anim, key, 0, 0);\n    if (r_is_objc_ptr(key)) ni_release_remote(key);\n}\n\nstatic void ni_overlay_hide(bool animated)\n{\n    uint64_t win = ni_overlay_window();\n    if (!r_is_objc_ptr(win)) return;\n    ni_cancel_scheduled_hide(win);\n\n    if (animated) {\n        ni_layer_opacity(win, 0.0f);\n        ni_layer_fade(win, 1.0, 0.0, kNIHideFadeSeconds);\n        if (!ni_schedule_hide(win, kNIHideFadeSeconds + 0.02)) {\n            r_msg2_main(win, "setHidden:", 1, 0, 0, 0);\n        }\n    } else {\n        r_msg2_main(win, "setHidden:", 1, 0, 0, 0);\n        ni_layer_opacity(win, 1.0f);\n    }\n}\n\nstatic void ni_destroy_overlay(void)\n{\n    uint64_t app = ni_springboard_application();\n    if (!r_is_objc_ptr(app)) return;\n    uint64_t win = ni_assoc_get(app, kNIWindowAssocKey);\n    if (r_is_objc_ptr(win)) {\n        ni_cancel_scheduled_hide(win);\n        r_msg2_main(win, "setHidden:", 1, 0, 0, 0);\n        r_msg2_main(win, "setRootViewController:", 0, 0, 0, 0);\n    }\n    ni_assoc_set(app, kNIWindowAssocKey, 0);\n    gNIOverlayPrepared = false;\n}\n\nstatic bool ni_overlay_create(void)\n{\n    uint64_t existing = ni_overlay_window();\n    if (r_is_objc_ptr(existing) &&\n        r_is_objc_ptr(ni_overlay_tagged(existing, kNITagIsland)) &&\n        r_is_objc_ptr(ni_overlay_tagged(existing, kNITagTitle))) {\n        gNIOverlayPrepared = true;\n        return true;\n    }\n    if (r_is_objc_ptr(existing)) ni_destroy_overlay();\n\n    uint64_t app = ni_springboard_application();\n    uint64_t keyWin = ni_key_window(app);\n    if (!r_is_objc_ptr(app) || !r_is_objc_ptr(keyWin)) return false;\n    uint64_t scene = r_msg2_main(keyWin, "windowScene", 0, 0, 0, 0);\n    if (!r_is_objc_ptr(scene)) return false;\n\n    NIRect64 keyBounds = {{0,0},{393,852}};\n    (void)r_msg2_main_struct_ret(keyWin, "bounds", &keyBounds, sizeof(keyBounds),\n                                 NULL, 0, NULL, 0, NULL, 0, NULL, 0);\n    double portraitWidth = fmin(keyBounds.size.width, keyBounds.size.height);\n    if (!isfinite(portraitWidth) || portraitWidth < 280.0 || portraitWidth > 700.0) portraitWidth = 393.0;\n\n    NIEdgeInsets64 insets = {59.0, 0, 0, 0};\n    (void)r_msg2_main_struct_ret(keyWin, "safeAreaInsets", &insets, sizeof(insets),\n                                 NULL, 0, NULL, 0, NULL, 0, NULL, 0);\n    double safeTop = (isfinite(insets.top) && insets.top >= 35.0 && insets.top <= 80.0)\n        ? insets.top : 59.0;\n\n    double winWidth = fmin(336.0, portraitWidth - 12.0);\n    double winHeight = 142.0;\n    double winX = (portraitWidth - winWidth) * 0.5;\n    double islandY = ni_clamp((safeTop - 39.0) * 0.5, 7.0, 14.0);\n    double contentY = islandY + 45.0;\n\n    NIRect64 windowFrame = {{winX, 0.0}, {winWidth, winHeight}};\n    NIRect64 localBounds = {{0.0, 0.0}, {winWidth, winHeight}};\n\n    uint64_t UIWindow = r_class("UIWindow");\n    uint64_t UIViewController = r_class("UIViewController");\n    uint64_t UIView = r_class("UIView");\n    uint64_t UIImageView = r_class("UIImageView");\n    uint64_t UIColor = r_class("UIColor");\n    if (!r_is_objc_ptr(UIWindow) || !r_is_objc_ptr(UIViewController) ||\n        !r_is_objc_ptr(UIView) || !r_is_objc_ptr(UIImageView) || !r_is_objc_ptr(UIColor)) {\n        return false;\n    }\n\n    uint64_t winAlloc = r_msg2_main(UIWindow, "alloc", 0, 0, 0, 0);\n    uint64_t win = r_is_objc_ptr(winAlloc)\n        ? r_msg2_main(winAlloc, "initWithWindowScene:", scene, 0, 0, 0)\n        : 0;\n    if (!r_is_objc_ptr(win)) return false;\n    ni_send_rect_main(win, "setFrame:", windowFrame);\n    ni_send_double_main(win, "setWindowLevel:", kNIWindowLevel);\n    uint64_t clearColor = r_msg2_main(UIColor, "clearColor", 0, 0, 0, 0);\n    uint64_t blackColor = r_msg2_main(UIColor, "blackColor", 0, 0, 0, 0);\n    r_msg2_main(win, "setBackgroundColor:", clearColor, 0, 0, 0);\n    r_msg2_main(win, "setOpaque:", 0, 0, 0, 0);\n\n    uint64_t vcAlloc = r_msg2_main(UIViewController, "alloc", 0, 0, 0, 0);\n    uint64_t vc = r_is_objc_ptr(vcAlloc) ? r_msg2_main(vcAlloc, "init", 0, 0, 0, 0) : 0;\n    if (!r_is_objc_ptr(vc)) {\n        ni_release_remote(win);\n        return false;\n    }\n    uint64_t root = r_msg2_main(vc, "view", 0, 0, 0, 0);\n    if (!r_is_objc_ptr(root)) {\n        ni_release_remote(vc);\n        ni_release_remote(win);\n        return false;\n    }\n    ni_send_rect_main(root, "setFrame:", localBounds);\n    r_msg2_main(root, "setBackgroundColor:", clearColor, 0, 0, 0);\n    r_msg2_main(root, "setUserInteractionEnabled:", 1, 0, 0, 0);\n\n    // Black capsule that visually owns the Dynamic Island area while the\n    // notification is visible. It does not stop the underlying Live Activity.\n    double islandWidth = fmin(220.0, winWidth - 44.0);\n    NIRect64 islandFrame = {{(winWidth - islandWidth) * 0.5, islandY}, {islandWidth, 39.0}};\n    uint64_t islandAlloc = r_msg2_main(UIView, "alloc", 0, 0, 0, 0);\n    uint64_t island = r_is_objc_ptr(islandAlloc)\n        ? r_msg2_main_raw(islandAlloc, "initWithFrame:", &islandFrame, sizeof(islandFrame), NULL, 0, NULL, 0, NULL, 0)\n        : 0;\n    if (r_is_objc_ptr(island)) {\n        r_msg2_main(island, "setBackgroundColor:", blackColor, 0, 0, 0);\n        r_msg2_main(island, "setTag:", kNITagIsland, 0, 0, 0);\n        uint64_t layer = r_msg2_main(island, "layer", 0, 0, 0, 0);\n        if (r_is_objc_ptr(layer)) {\n            ni_send_double_main(layer, "setCornerRadius:", 19.5);\n            r_msg2_main(layer, "setMasksToBounds:", 1, 0, 0, 0);\n        }\n        r_msg2_main(root, "addSubview:", island, 0, 0, 0);\n    }\n\n    // Transparent content region. Only text/icon themselves draw; there is no\n    // backing card or blur rectangle underneath them.\n    NIRect64 iconFrame = {{17.0, contentY + 6.0}, {38.0, 38.0}};\n    uint64_t iconAlloc = r_msg2_main(UIImageView, "alloc", 0, 0, 0, 0);\n    uint64_t icon = r_is_objc_ptr(iconAlloc)\n        ? r_msg2_main_raw(iconAlloc, "initWithFrame:", &iconFrame, sizeof(iconFrame), NULL, 0, NULL, 0, NULL, 0)\n        : 0;\n    if (r_is_objc_ptr(icon)) {\n        r_msg2_main(icon, "setTag:", kNITagIcon, 0, 0, 0);\n        r_msg2_main(icon, "setContentMode:", 1, 0, 0, 0); // scaleAspectFit\n        uint64_t layer = r_msg2_main(icon, "layer", 0, 0, 0, 0);\n        if (r_is_objc_ptr(layer)) {\n            ni_send_double_main(layer, "setCornerRadius:", 9.0);\n            r_msg2_main(layer, "setMasksToBounds:", 1, 0, 0, 0);\n        }\n        r_msg2_main(root, "addSubview:", icon, 0, 0, 0);\n    }\n\n    uint64_t secondary = ni_color_white_alpha(1.0, 0.74);\n    uint64_t bodyColor = ni_color_white_alpha(1.0, 0.90);\n    uint64_t whiteColor = r_msg2_main(UIColor, "whiteColor", 0, 0, 0, 0);\n    double textX = 64.0;\n    double textW = winWidth - textX - 14.0;\n\n    NIRect64 sourceFrame = {{textX, contentY + 1.0}, {textW, 17.0}};\n    NIRect64 titleFrame  = {{textX, contentY + 18.0}, {textW, 22.0}};\n    NIRect64 bodyFrame   = {{textX, contentY + 40.0}, {textW, 35.0}};\n    uint64_t sourceLabel = ni_make_label(sourceFrame, 12.0, true, secondary, kNITagSource);\n    uint64_t titleLabel = ni_make_label(titleFrame, 15.5, true, whiteColor, kNITagTitle);\n    uint64_t bodyLabel = ni_make_label(bodyFrame, 13.5, false, bodyColor, kNITagBody);\n    if (r_is_objc_ptr(sourceLabel)) {\n        r_msg2_main(sourceLabel, "setNumberOfLines:", 1, 0, 0, 0);\n        r_msg2_main(root, "addSubview:", sourceLabel, 0, 0, 0);\n    }\n    if (r_is_objc_ptr(titleLabel)) {\n        r_msg2_main(titleLabel, "setNumberOfLines:", 1, 0, 0, 0);\n        r_msg2_main(root, "addSubview:", titleLabel, 0, 0, 0);\n    }\n    if (r_is_objc_ptr(bodyLabel)) {\n        r_msg2_main(bodyLabel, "setNumberOfLines:", 2, 0, 0, 0);\n        r_msg2_main(root, "addSubview:", bodyLabel, 0, 0, 0);\n    }\n\n    r_msg2_main(win, "setRootViewController:", vc, 0, 0, 0);\n    r_msg2_main(win, "setHidden:", 1, 0, 0, 0);\n\n    // The swipe action is a retained NSInvocation that calls setHidden:YES on\n    // the overlay window. NSInvocation tolerates the extra sender argument used\n    // by target/action dispatch on arm64, matching the same pattern Cyanide uses\n    // for remote notification observers elsewhere.\n    uint8_t hidden = 1;\n    NIInvocationArg hideArgs[] = {{&hidden, sizeof(hidden)}};\n    uint64_t hideInvocation = ni_invocation(win, "setHidden:", hideArgs, 1);\n    if (r_is_objc_ptr(hideInvocation)) {\n        ni_assoc_set(win, kNIHideInvocationAssocKey, hideInvocation);\n        uint64_t UISwipeGestureRecognizer = r_class("UISwipeGestureRecognizer");\n        uint64_t invokeSel = r_sel("invoke");\n        uint64_t gestureAlloc = r_is_objc_ptr(UISwipeGestureRecognizer)\n            ? r_msg2_main(UISwipeGestureRecognizer, "alloc", 0, 0, 0, 0)\n            : 0;\n        uint64_t gesture = (r_is_objc_ptr(gestureAlloc) && invokeSel)\n            ? r_msg2_main(gestureAlloc, "initWithTarget:action:", hideInvocation, invokeSel, 0, 0)\n            : 0;\n        if (r_is_objc_ptr(gesture)) {\n            r_msg2_main(gesture, "setDirection:", 4 /* UISwipeGestureRecognizerDirectionUp */, 0, 0, 0);\n            r_msg2_main(gesture, "setCancelsTouchesInView:", 1, 0, 0, 0);\n            r_msg2_main(root, "addGestureRecognizer:", gesture, 0, 0, 0);\n            ni_release_remote(gesture);\n        }\n        ni_release_remote(hideInvocation);\n    }\n\n    bool associated = ni_assoc_set(app, kNIWindowAssocKey, win);\n    // Drop our +1 from alloc/init. If association succeeded, the associated\n    // object owns the window; otherwise this also cleans up the failed build.\n    ni_release_remote(win);\n    ni_release_remote(island);\n    ni_release_remote(icon);\n    ni_release_remote(sourceLabel);\n    ni_release_remote(titleLabel);\n    ni_release_remote(bodyLabel);\n    ni_release_remote(vc);\n\n    gNIOverlayPrepared = associated;\n    printf("[NISLAND] Siri overlay create=%d width=%.1f islandY=%.1f safeTop=%.1f\\n",\n           associated, winWidth, islandY, safeTop);\n    return associated;\n}\n\nstatic void ni_set_remote_text(uint64_t label, const char *text)\n{\n    if (!r_is_objc_ptr(label)) return;\n    uint64_t str = r_nsstr_retained(text ? text : "");\n    if (r_is_objc_ptr(str)) {\n        r_msg2_main(label, "setText:", str, 0, 0, 0);\n        ni_release_remote(str);\n    }\n}\n\nstatic uint64_t ni_application_icon(const char *bundle)\n{\n    uint64_t UIImage = r_class("UIImage");\n    if (!r_is_objc_ptr(UIImage)) return 0;\n\n    uint64_t image = 0;\n    if (bundle && bundle[0]) {\n        uint64_t bundleString = r_nsstr_retained(bundle);\n        if (r_is_objc_ptr(bundleString) &&\n            r_responds_main(UIImage, "_applicationIconImageForBundleIdentifier:format:scale:")) {\n            uint64_t format = 2;\n            double scale = 3.0;\n            image = r_msg2_main_raw(UIImage,\n                                    "_applicationIconImageForBundleIdentifier:format:scale:",\n                                    &bundleString, sizeof(bundleString),\n                                    &format, sizeof(format),\n                                    &scale, sizeof(scale),\n                                    NULL, 0);\n            if (!r_is_objc_ptr(image)) {\n                format = 1;\n                image = r_msg2_main_raw(UIImage,\n                                        "_applicationIconImageForBundleIdentifier:format:scale:",\n                                        &bundleString, sizeof(bundleString),\n                                        &format, sizeof(format),\n                                        &scale, sizeof(scale),\n                                        NULL, 0);\n            }\n        }\n        if (r_is_objc_ptr(bundleString)) ni_release_remote(bundleString);\n    }\n\n    if (!r_is_objc_ptr(image) && r_responds_main(UIImage, "systemImageNamed:")) {\n        uint64_t bell = r_nsstr_retained("bell.fill");\n        if (r_is_objc_ptr(bell)) {\n            image = r_msg2_main(UIImage, "systemImageNamed:", bell, 0, 0, 0);\n            ni_release_remote(bell);\n        }\n    }\n    return image;\n}\n\nstatic bool ni_overlay_update(const char *bundle,\n                              const char *source,\n                              const char *title,\n                              const char *body)\n{\n    if (!ni_overlay_create()) return false;\n    uint64_t win = ni_overlay_window();\n    if (!r_is_objc_ptr(win)) return false;\n\n    uint64_t icon = ni_overlay_tagged(win, kNITagIcon);\n    uint64_t sourceLabel = ni_overlay_tagged(win, kNITagSource);\n    uint64_t titleLabel = ni_overlay_tagged(win, kNITagTitle);\n    uint64_t bodyLabel = ni_overlay_tagged(win, kNITagBody);\n    if (!r_is_objc_ptr(titleLabel)) return false;\n\n    uint64_t image = ni_application_icon(bundle);\n    if (r_is_objc_ptr(icon)) r_msg2_main(icon, "setImage:", image, 0, 0, 0);\n    ni_set_remote_text(sourceLabel, source);\n    ni_set_remote_text(titleLabel, title);\n    ni_set_remote_text(bodyLabel, body);\n    if (r_is_objc_ptr(bodyLabel)) {\n        r_msg2_main(bodyLabel, "setHidden:", (body && body[0]) ? 0 : 1, 0, 0, 0);\n    }\n\n    ni_cancel_scheduled_hide(win);\n    r_msg2_main(win, "setHidden:", 0, 0, 0, 0);\n    ni_layer_opacity(win, 1.0f);\n    ni_layer_fade(win, 0.0, 1.0, kNIShowFadeSeconds);\n    return true;\n}\n\n#pragma mark - Retire old ActivityKit presentation\n\nstatic void ni_end_legacy_activitykit_local(void)\n{\n    Class cls = NSClassFromString(@"NotificationIslandLiveActivityBridge");\n    SEL sel = NSSelectorFromString(@"end");\n    if (cls && sel && [cls respondsToSelector:sel]) {\n        ((void (*)(id, SEL))objc_msgSend)(cls, sel);\n    }\n}\n\n#pragma mark - Public API\n\nbool notificationisland_apply_in_session(void)\n{\n    ni_end_legacy_activitykit_local();\n    ni_destroy_overlay();\n    uint32_t oldSettle = r_settle_us(2000);\n    bool ok = false;\n    @try {\n        ok = ni_overlay_create();\n    } @finally {\n        r_settle_us(oldSettle);\n    }\n    if (ok) {\n        log_user("[NISLAND] Siri-style overlay ready. Portrait uses Notification Island; landscape keeps native banners.\\n");\n    } else {\n        log_user("[NISLAND] Siri-style overlay could not be created in SpringBoard.\\n");\n    }\n    return ok;\n}\n\nbool notificationisland_tick_in_session(void)\n{\n    uint64_t nowUS = ni_now_us();\n\n    // Foreground orientation comes from SpringBoard\'s frontmost-app orientation,\n    // not SpringBoard\'s own scene orientation. This is important for landscape\n    // games whose SpringBoard scene can stay portrait.\n    if (ni_frontmost_is_landscape()) {\n        if (gNIVisibleUntilUS != 0) {\n            uint32_t oldSettle = r_settle_us(2000);\n            ni_overlay_hide(false);\n            r_settle_us(oldSettle);\n        }\n        gNIVisibleUntilUS = 0;\n        gNILastRequest = 0;\n        // Crucially: do not withdraw the banner in landscape.\n        return true;\n    }\n\n    uint32_t oldSettle = r_settle_us(2000);\n    bool result = true;\n    @try {\n        if (gNIVisibleUntilUS != 0 && nowUS >= gNIVisibleUntilUS) {\n            ni_overlay_hide(true);\n            gNIVisibleUntilUS = 0;\n        }\n\n        uint64_t app = ni_springboard_application();\n        if (!r_is_objc_ptr(app)) {\n            result = false;\n        } else {\n            uint64_t dispatcher = ni_springboard_dispatcher(app);\n            uint64_t banner = ni_banner_destination(dispatcher);\n            uint64_t req = ni_active_notification_request(banner);\n            if (!r_is_objc_ptr(req)) {\n                gNILastRequest = 0;\n            } else {\n                char identifier[sizeof(gNILastIdentifier)] = {0};\n                char bundle[192] = {0};\n                char source[192] = {0};\n                char title[256] = {0};\n                char body[512] = {0};\n                if (ni_request_text(req,\n                                    identifier, sizeof(identifier),\n                                    bundle, sizeof(bundle),\n                                    source, sizeof(source),\n                                    title, sizeof(title),\n                                    body, sizeof(body))) {\n                    uint64_t contentHash = ni_content_hash(source, title, body);\n                    bool samePtr = (gNILastRequest == req);\n                    bool sameID = identifier[0] && strcmp(identifier, gNILastIdentifier) == 0;\n                    bool sameContent = (contentHash == gNILastContentHash);\n                    bool recentSameID = sameID && sameContent && gNILastShowUS != 0 &&\n                        nowUS >= gNILastShowUS && (nowUS - gNILastShowUS) < kNIRepeatGuardUS;\n\n                    // Same remote request + same visible content is just the\n                    // polling loop seeing the same banner again. Keep it hidden\n                    // underneath our overlay and, if the user swiped our overlay\n                    // away, do not resurrect it from the same request.\n                    if ((samePtr && sameContent) || recentSameID) {\n                        (void)ni_withdraw_banner_only(banner, req);\n                    } else {\n                        bool shown = ni_overlay_update(bundle, source, title, body);\n                        bool withdrew = shown ? ni_withdraw_banner_only(banner, req) : false;\n                        if (shown) {\n                            gNIVisibleUntilUS = nowUS + kNIVisibleUS;\n                            gNILastShowUS = nowUS;\n                            gNILastContentHash = contentHash;\n                            snprintf(gNILastIdentifier, sizeof(gNILastIdentifier), "%s", identifier);\n                            gNILastRequest = req;\n                        }\n                        printf("[NISLAND] siri route id=%s req=0x%llx shown=%d withdrawBanner=%d bundle=\'%s\' source=\'%s\' title=\'%s\'\\n",\n                               identifier,\n                               (unsigned long long)req,\n                               shown,\n                               withdrew,\n                               bundle,\n                               source,\n                               title);\n                    }\n                }\n            }\n        }\n    } @finally {\n        r_settle_us(oldSettle);\n    }\n    return result;\n}\n\nbool notificationisland_show_sample_in_session(const char *title, const char *body)\n{\n    uint32_t oldSettle = r_settle_us(2000);\n    bool ok = false;\n    @try {\n        ok = ni_overlay_update("", "Cyanide", title && title[0] ? title : "Notification Island",\n                               body && body[0] ? body : "Siri-style transparent notification preview");\n        if (ok) {\n            uint64_t nowUS = ni_now_us();\n            gNIVisibleUntilUS = nowUS + kNIVisibleUS;\n            gNILastShowUS = nowUS;\n        }\n    } @finally {\n        r_settle_us(oldSettle);\n    }\n    return ok;\n}\n\nbool notificationisland_stop_in_session(void)\n{\n    ni_end_legacy_activitykit_local();\n    uint32_t oldSettle = r_settle_us(2000);\n    @try {\n        ni_destroy_overlay();\n    } @finally {\n        r_settle_us(oldSettle);\n    }\n    gNIVisibleUntilUS = 0;\n    gNILastRequest = 0;\n    gNILastShowUS = 0;\n    gNILastContentHash = 0;\n    gNILastIdentifier[0] = \'\\0\';\n    return true;\n}\n\nvoid notificationisland_forget_remote_state(void)\n{\n    gNIOverlayPrepared = false;\n    gNILastRequest = 0;\n    gNIVisibleUntilUS = 0;\n    gNILastShowUS = 0;\n    gNILastContentHash = 0;\n    gNILastIdentifier[0] = \'\\0\';\n    gNIMissingBannerLogged = false;\n    gNIMissingDispatcherLogged = false;\n    gNILastFrontOrientation = 1;\n}\n\nbool notificationisland_has_remote_state(void)\n{\n    return gNIOverlayPrepared;\n}\n'

def main():
    root = Path(sys.argv[1] if len(sys.argv) > 1 else '.').resolve()
    target = root / 'Cyanide' / 'tweaks' / 'experimental' / 'notificationisland.m'
    if not target.parent.exists():
        raise SystemExit(f'[NISLAND] missing target directory: {target.parent}')
    target.write_text(SOURCE, encoding='utf-8')
    print(f'[NISLAND] installed Siri-style Notification Island -> {target}')

if __name__ == '__main__':
    main()
